{
  "name": "opencode-manager",
  "version": "0.11.0",
  "private": false,
  "type": "module",
  "packageManager": "pnpm@10.28.1",
  "scripts": {
    "predev": "bash scripts/setup-dev.sh",
    "dev": "concurrently \"pnpm:dev:backend\" \"pnpm:dev:frontend\"",
    "dev:backend": "NODE_ENV=development bun --watch-path backend/src --watch backend/src/index.ts",
    "dev:frontend": "pnpm --filter frontend dev",
    "start": "concurrently \"pnpm:start:backend\" \"pnpm:start:frontend\"",
    "start:backend": "bun run backend/dist/index.js",
    "start:frontend": "pnpm --filter frontend preview",
    "build": "pnpm run build:backend && pnpm run build:frontend",
    "build:backend": "pnpm --filter backend build",
    "build:frontend": "pnpm --filter frontend build",
    "typecheck": "pnpm run typecheck:frontend && pnpm run typecheck:backend",
    "typecheck:frontend": "pnpm --filter frontend typecheck",
    "typecheck:backend": "pnpm --filter backend typecheck",
    "test": "pnpm run test:backend && pnpm run test:frontend",
    "test:frontend": "pnpm --filter frontend exec vitest run",
    "test:backend": "pnpm --filter backend run test",
    "lint": "pnpm run lint:frontend && pnpm run lint:backend",
    "lint:frontend": "pnpm --filter frontend lint",
    "lint:backend": "pnpm --filter backend lint",
    "lint:fix": "pnpm run lint:frontend -- --fix && pnpm run lint:backend -- --fix",
    "generate:openapi": "bun scripts/generate-openapi.ts",
    "test:cli": "pnpm --filter @opencode-manager/ocm-cli test",
    "typecheck:cli": "pnpm --filter @opencode-manager/ocm-cli typecheck",
    "docker:build": "docker-compose build",
    "docker:up": "docker-compose up -d",
    "docker:down": "docker-compose down -v",
    "docker:logs": "docker-compose logs -f",
    "docker:restart": "docker-compose restart"
  },
  "devDependencies": {
    "concurrently": "^9.1.0"
  },
  "dependencies": {
    "@radix-ui/react-tooltip": "^1.2.8",
    "class-variance-authority": "^0.7.1"
  }
}
